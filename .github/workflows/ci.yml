name: CI

on:
  pull_request:
  workflow_call:
    inputs:
      release_project:
        description: Public project key used to isolate concurrent release verification.
        required: false
        type: string
        default: ''
      release_build:
        description: Disable dependency caching and retain release-ready package artifacts.
        required: false
        type: boolean
        default: false
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}-${{ inputs.release_project }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

env:
  NODE_VERSION: 24.15.0
  PNPM_VERSION: 11.9.0

jobs:
  verify:
    name: Verify Repository
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.62.1-noble
      options: --ipc=host
    env:
      PLAYWRIGHT_BROWSERS_PATH: /ms-playwright
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Trust Container Workspace for Git
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js with dependency caching
        if: ${{ inputs.release_build != true }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Setup Node.js for a release build
        if: ${{ inputs.release_build == true }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Check Changed Package Versions
        if: ${{ github.event_name == 'pull_request' }}
        env:
          RELEASE_BASE_COMMIT: ${{ github.event.pull_request.base.sha }}
          RELEASE_CURRENT_COMMIT: ${{ github.event.pull_request.head.sha }}
        run: >-
          pnpm release:check-changes
          "$RELEASE_BASE_COMMIT"
          "$RELEASE_CURRENT_COMMIT"

      - name: Check Runtime Compatibility Artifacts
        run: pnpm compatibility:check

      - name: Check Package Documentation Sources
        run: pnpm docs:check

      - name: Check Formatting
        run: pnpm format:check

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      - name: Build
        run: pnpm build

      - name: Test
        run: pnpm test

      - name: Pack Public Package Artifacts
        run: |
          public_packages_directory="$GITHUB_WORKSPACE/artifacts/public-package-tarballs"
          mkdir -p "$public_packages_directory"
          pnpm --dir projects/repository pack --pack-destination "$public_packages_directory"
          pnpm --dir projects/repository-fs pack --pack-destination "$public_packages_directory"
          pnpm --dir projects/core pack --pack-destination "$public_packages_directory"
          pnpm --dir projects/adapter-anthropic pack --pack-destination "$public_packages_directory"
          pnpm --dir projects/adapter-google-genai pack --pack-destination "$public_packages_directory"
          pnpm --dir projects/adapter-openai pack --pack-destination "$public_packages_directory"
          pnpm --dir projects/adapter-openai-agents-sdk pack --pack-destination "$public_packages_directory"
          pnpm --dir projects/adapter-claude-agent-sdk pack --pack-destination "$public_packages_directory"
          pnpm --dir projects/adapter-cloudflare-agents pack --pack-destination "$public_packages_directory"
          pnpm --dir projects/adapter-eve pack --pack-destination "$public_packages_directory"
          pnpm --dir projects/adapter-langchain pack --pack-destination "$public_packages_directory"
          pnpm --dir projects/adapter-langgraph pack --pack-destination "$public_packages_directory"
          pnpm --dir projects/adapter-vercel-ai-sdk pack --pack-destination "$public_packages_directory"
          pnpm --dir projects/cli pack --pack-destination "$public_packages_directory"
          pnpm --dir projects/website-ui pack --pack-destination "$public_packages_directory"
          pnpm release:checksums create "$public_packages_directory"

          expected_tarball_count=15
          actual_tarball_count=$(find "$public_packages_directory" -maxdepth 1 -type f -name '*.tgz' | wc -l)
          if [ "$actual_tarball_count" -ne "$expected_tarball_count" ]; then
            echo "Expected $expected_tarball_count package tarballs, found $actual_tarball_count." >&2
            exit 1
          fi

      - name: Upload Public Package Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: public-package-tarballs
          path: |
            artifacts/public-package-tarballs/*.tgz
            artifacts/public-package-tarballs/SHA256SUMS
          if-no-files-found: error
          compression-level: 0
          retention-days: 7

  repository-testing-peer-compatibility:
    name: Repository Testing Peers (Vitest ${{ matrix.vitest-version }})
    needs: verify
    runs-on: ubuntu-latest
    env:
      PNPM_VERSION: 11.9.0
      WEB_UTILS_KIT_VERSION: 1.3.1
    strategy:
      fail-fast: false
      matrix:
        vitest-version:
          - 1.0.0
          - 2.0.0
          - 3.2.4
          - 4.1.10
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Download Public Package Artifacts
        uses: actions/download-artifact@v8
        with:
          name: 'public-package-tarballs'
          path: ${{ runner.temp }}/public-package-tarballs

      - name: Test Packed Repository Testing Peers
        run: >-
          node projects/repository/scripts/testing-peer-compatibility/index.mjs
          "${{ runner.temp }}/public-package-tarballs"
          "${{ matrix.vitest-version }}"

  cli-testing-peer-compatibility:
    name: CLI Existing Vitest 3.2.4 Compatibility
    needs: verify
    runs-on: ubuntu-latest
    env:
      PNPM_VERSION: 11.21.0
      VITEST_VERSION: 3.2.4
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Download Public Package Artifacts
        uses: actions/download-artifact@v8
        with:
          name: 'public-package-tarballs'
          path: ${{ runner.temp }}/public-package-tarballs

      - name: Test Packed CLI with Existing Vitest
        run: >-
          node projects/cli/scripts/testing-peer-compatibility/index.mjs
          "${{ runner.temp }}/public-package-tarballs"
          "${{ env.VITEST_VERSION }}"

  compatibility-bridge-packages:
    name: Verify 4.0.2 Package Compatibility Bridge
    needs: verify
    if: ${{ inputs.release_build == true }}
    runs-on: ubuntu-latest
    env:
      PACKAGES_SOURCE_BASELINE_COMMIT: 9fa7a2ba4d3455b6310a62ece172571ab473f745
      PACKAGES_SOURCE_BASELINE_TREE: 9eba53f4f54185432b04a4f9a4fac44543b5d24b
    steps:
      - name: Checkout Packages Candidate
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          path: packages
          persist-credentials: false
          ref: ${{ github.sha }}

      - name: Checkout Frozen Skill Comparator
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          path: skill
          persist-credentials: false
          ref: 69702faa1fcfe4c36fb05ec9ea1ffa0a82bd00e7 # compatibility-bridge-4.0.2 comparator
          repository: moldea-ai/skill

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install Frozen Comparator Dependencies
        run: npm --prefix skill ci --ignore-scripts

      - name: Download Public Package Artifacts
        uses: actions/download-artifact@v8
        with:
          name: 'public-package-tarballs'
          path: ${{ runner.temp }}/public-package-tarballs

      - name: Compare 4.0.2 Package Artifacts
        run: >-
          npm --prefix skill run release:compatibility-bridge:check-packages --
          --packages-repository "$GITHUB_WORKSPACE/packages"
          --artifact-directory "${{ runner.temp }}/public-package-tarballs"
          --packages-commit "${{ github.sha }}"

  repository-fs-runtime-compatibility:
    name: Repository FS Runtime (Node.js ${{ matrix.node-version }})
    needs: verify
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 22.11.0
          - 22.x
          - 24.11.0
          - 24.x
          - 26.8.1
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - name: Download Repository FS Runtime Artifacts
        uses: actions/download-artifact@v8
        with:
          name: public-package-tarballs
          path: ${{ runner.temp }}/public-package-tarballs

      - name: Test Packed Repository FS Runtime
        run: >-
          node projects/repository-fs/scripts/runtime-compatibility/index.mjs
          "${{ runner.temp }}/public-package-tarballs"

  cli-runtime-compatibility:
    name: CLI Runtime (Node.js ${{ matrix.node-version }})
    needs: verify
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 22.11.0
          - 22.x
          - 24.11.0
          - 24.x
          - 26.8.1
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - name: Download CLI Runtime Artifacts
        uses: actions/download-artifact@v8
        with:
          name: public-package-tarballs
          path: ${{ runner.temp }}/public-package-tarballs

      - name: Test Packed CLI Runtime
        run: >-
          node projects/cli/scripts/runtime-compatibility/index.mjs
          "${{ runner.temp }}/public-package-tarballs"

  adapter-openai-runtime-compatibility:
    name: OpenAI Adapter Runtime (Node.js ${{ matrix.node-version }})
    needs: verify
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 22.11.0
          - 22.x
          - 24.11.0
          - 24.x
          - 26.8.1
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - name: Download OpenAI Adapter Runtime Artifacts
        uses: actions/download-artifact@v8
        with:
          name: public-package-tarballs
          path: ${{ runner.temp }}/public-package-tarballs

      - name: Test Packed OpenAI Adapter Runtime
        run: >-
          node projects/adapter-openai/scripts/runtime-compatibility/index.mjs
          "${{ runner.temp }}/public-package-tarballs"

  adapter-google-genai-runtime-compatibility:
    name: Google Gen AI Adapter Runtime (Node.js ${{ matrix.node-version }})
    needs: verify
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 22.11.0
          - 22.x
          - 24.11.0
          - 24.x
          - 26.8.1
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - name: Download Google Gen AI Adapter Runtime Artifacts
        uses: actions/download-artifact@v8
        with:
          name: public-package-tarballs
          path: ${{ runner.temp }}/public-package-tarballs

      - name: Test Packed Google Gen AI Adapter Runtime
        run: >-
          node projects/adapter-google-genai/scripts/runtime-compatibility/index.mjs
          "${{ runner.temp }}/public-package-tarballs"

  adapter-openai-agents-sdk-runtime-compatibility:
    name: OpenAI Agents SDK Adapter Runtime (Node.js ${{ matrix.node-version }})
    needs: verify
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 22.11.0
          - 22.x
          - 24.11.0
          - 24.x
          - 26.8.1
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - name: Download OpenAI Agents SDK Adapter Runtime Artifacts
        uses: actions/download-artifact@v8
        with:
          name: public-package-tarballs
          path: ${{ runner.temp }}/public-package-tarballs

      - name: Test Packed OpenAI Agents SDK Adapter Runtime
        run: >-
          node projects/adapter-openai-agents-sdk/scripts/runtime-compatibility/index.mjs
          "${{ runner.temp }}/public-package-tarballs"

  adapter-claude-agent-sdk-runtime-compatibility:
    name: Claude Agent SDK Adapter Runtime (Node.js ${{ matrix.node-version }})
    needs: verify
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 22.11.0
          - 22.x
          - 24.11.0
          - 24.x
          - 26.8.1
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - name: Download Claude Agent SDK Adapter Runtime Artifacts
        uses: actions/download-artifact@v8
        with:
          name: public-package-tarballs
          path: ${{ runner.temp }}/public-package-tarballs

      - name: Test Packed Claude Agent SDK Adapter Runtime
        run: >-
          node projects/adapter-claude-agent-sdk/scripts/runtime-compatibility/index.mjs
          "${{ runner.temp }}/public-package-tarballs"

  adapter-cloudflare-agents-runtime-compatibility:
    name: Cloudflare Agents Adapter Runtime (Node.js ${{ matrix.node-version }})
    needs: verify
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 22.11.0
          - 22.x
          - 24.11.0
          - 24.x
          - 26.8.1
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - name: Download Cloudflare Agents Adapter Runtime Artifacts
        uses: actions/download-artifact@v8
        with:
          name: public-package-tarballs
          path: ${{ runner.temp }}/public-package-tarballs

      - name: Test Packed Cloudflare Agents Adapter Runtime
        run: >-
          node projects/adapter-cloudflare-agents/scripts/runtime-compatibility/index.mjs
          "${{ runner.temp }}/public-package-tarballs"

  adapter-vercel-ai-sdk-runtime-compatibility:
    name: Vercel AI SDK Adapter Runtime (Node.js ${{ matrix.node-version }})
    needs: verify
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 22.11.0
          - 22.x
          - 24.11.0
          - 24.x
          - 26.8.1
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - name: Download Vercel AI SDK Adapter Runtime Artifacts
        uses: actions/download-artifact@v8
        with:
          name: public-package-tarballs
          path: ${{ runner.temp }}/public-package-tarballs

      - name: Test Packed Vercel AI SDK Adapter Runtime
        run: >-
          node projects/adapter-vercel-ai-sdk/scripts/runtime-compatibility/index.mjs
          "${{ runner.temp }}/public-package-tarballs"

  adapter-eve-runtime-compatibility:
    name: Eve Adapter Runtime (Node.js ${{ matrix.node-version }})
    needs: verify
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 22.11.0
          - 22.x
          - 24.11.0
          - 24.x
          - 26.8.1
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - name: Download Eve Adapter Runtime Artifacts
        uses: actions/download-artifact@v8
        with:
          name: public-package-tarballs
          path: ${{ runner.temp }}/public-package-tarballs

      - name: Test Packed Eve Adapter Runtime
        run: >-
          node projects/adapter-eve/scripts/runtime-compatibility/index.mjs
          "${{ runner.temp }}/public-package-tarballs"

  adapter-langchain-runtime-compatibility:
    name: LangChain Adapter Runtime (Node.js ${{ matrix.node-version }})
    needs: verify
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 22.11.0
          - 22.x
          - 24.11.0
          - 24.x
          - 26.8.1
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - name: Download LangChain Adapter Runtime Artifacts
        uses: actions/download-artifact@v8
        with:
          name: public-package-tarballs
          path: ${{ runner.temp }}/public-package-tarballs

      - name: Test Packed LangChain Adapter Runtime
        run: >-
          node projects/adapter-langchain/scripts/runtime-compatibility/index.mjs
          "${{ runner.temp }}/public-package-tarballs"

  adapter-langgraph-runtime-compatibility:
    name: LangGraph Adapter Runtime (Node.js ${{ matrix.node-version }})
    needs: verify
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 22.11.0
          - 22.x
          - 24.11.0
          - 24.x
          - 26.8.1
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - name: Download LangGraph Adapter Runtime Artifacts
        uses: actions/download-artifact@v8
        with:
          name: public-package-tarballs
          path: ${{ runner.temp }}/public-package-tarballs

      - name: Test Packed LangGraph Adapter Runtime
        run: >-
          node projects/adapter-langgraph/scripts/runtime-compatibility/index.mjs
          "${{ runner.temp }}/public-package-tarballs"

  adapter-anthropic-runtime-compatibility:
    name: Anthropic Adapter Runtime (Node.js ${{ matrix.node-version }})
    needs: verify
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 22.11.0
          - 22.x
          - 24.11.0
          - 24.x
          - 26.8.1
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - name: Download Anthropic Adapter Runtime Artifacts
        uses: actions/download-artifact@v8
        with:
          name: public-package-tarballs
          path: ${{ runner.temp }}/public-package-tarballs

      - name: Test Packed Anthropic Adapter Runtime
        run: >-
          node projects/adapter-anthropic/scripts/runtime-compatibility/index.mjs
          "${{ runner.temp }}/public-package-tarballs"

  test-cross-platform:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - macos-latest
          - windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js with dependency caching
        if: ${{ inputs.release_build != true }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Setup Node.js for a release build
        if: ${{ inputs.release_build == true }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Website Browser
        run: pnpm --filter @moldea.ai/packages-website exec playwright install chromium

      - name: Build
        run: pnpm build

      - name: Test
        run: pnpm test
